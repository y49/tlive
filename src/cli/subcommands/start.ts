// src/cli/subcommands/start.ts
//
// Stub for Phase 6.3. Phase 7 fills in detached spawn logic.
// For now, this just throws so the test mock has something to mock against.

export async function runStart(_argv: string[]): Promise<void> {
  throw new Error('runStart: full implementation deferred to Phase 7');
}
